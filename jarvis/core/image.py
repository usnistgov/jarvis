

class Image(object):
  def __init__(self,arr=[],pbc_params=[]):
      # pbc_params for 2D image: a,b and gamma
      # 3D : a,b,c, alpha, beta, gamma

      self.arr=arr #XY numpy array
      self.pbc_params=pbc_params

  def fourier_transform(self):
      ft_arr=
      return ft_arr

  def get_ft_parameters(self):
      
      return f_params

  def replicate(self,scell=[1,1]):

  def affine_tranform(self,grid=[]):

  def load_from_jpeg(self):

  def load_from_png(self):

  def save(self)
 
  def color_to_black(self):

  def filtering(self):

  def rotation(self):
